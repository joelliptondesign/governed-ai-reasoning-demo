Example Artifact

The file `governed_eval_example.json` illustrates the output of the full governed reasoning pipeline.

Key sections:

critic_review
Structured evaluation of diagnosis quality.

governance_result
Policy-based arbitration of critic scores.

final_output
The final governed decision produced by the pipeline.

This artifact demonstrates how probabilistic reasoning can be transformed into a deterministic decision.
